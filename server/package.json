{
  "name": "server",
  "version": "1.0.0",
  "description": "skripts server",
  "main": "main.js",
  "scripts": {
    "dev": "DEBUG=1 nodemon main.js --ignore datastore/fs/scripts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "randomstring": "^1.1.5"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
